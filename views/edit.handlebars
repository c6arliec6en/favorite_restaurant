<div class="container">
  <div class="row">
    <div class="col-12 col-md-10 mx-auto">
      <form action="/edit/{{{res.id}}}" method="POST">
        <p class="mb-1">
          <span class="text-secondary">
            <i class="fas fa-utensils pr-2"></i>
            姓名：
          </span>
          <input type="text" name="name" value="{{{res.name}}}" class="edit-input">
        </p>
        <p class="mb-1">
          <span class="text-secondary">
            <i class="fas fa-list-ul pr-1"></i>
            類別：
          </span>
          <input type="text" name="category" value="{{{res.category}}}" class="edit-input">
        </p>

        <p class="mb-1">
          <span class="text-secondary">
            <i class="fas fa-map-marker-alt pr-2"></i>
            地址：
          </span>
          <input type="text" name="location" value="{{{res.location}}}" class="edit-input">
          <a href="https://goo.gl/maps/V9mKwVJ4s5v" class="text-secondary" target="_blank">
            <i class="fas fa-location-arrow pr-2 fa-xs"></i>
          </a>
        </p>

        <p class="mb-1">
          <span class="text-secondary">
            <i class="fas fa-mobile-alt pr-2"></i>
            電話：
          </span>
          <input type="text" name="phone" value="{{{res.phone}}}" class="edit-input">
        </p>

        <p class="mb-2">
          <span class="text-secondary">
            <i class="fas fa-camera pr-1"></i>
            圖片：
          </span>
          <input type="text" name="image" value="{{{res.image}}}" class="edit-input">
        </p>
        <p class="mb-1">
          <span class="text-secondary">
            <i class="fas fa-file-signature"></i>
            餐廳簡述：
          </span></p>

        <p class="mb-3">
          <textarea name="description" cols="100" rows="5" style="resize:none">{{{res.description}}}</textarea>
        </p>
        <p class="mb-5">
          <button type="submit" class="btn ok">確認送出</button>
        </p>

      </form>

    </div>
  </div>
</div>